{
  "name": "User",
  "type": "object",
  "properties": {
    "role": {
      "type": "string",
      "description": "The role of the user in the app",
      "enum": [
        "admin",
        "user"
      ]
    },
    "client_id": {
      "type": "string",
      "description": "Reference to the Client entity for portal users"
    }
  },
  "required": [
    "role"
  ]
}